# Additional packages needed for standalone (AstrBot) mode.
# These are transitive deps of nonebot2/nonebot-plugin-* that
# the upstream code uses directly but doesn't list in requirements.txt
# because the NoneBot environment provides them.

httpx>=0.28.0,<1.0.0
httpx[socks]>=0.28.0,<1.0.0
qrcode>=7.0,<9.0
yarl>=1.0,<2.0
pydantic>=2.0,<3.0
typing_extensions>=4.0,<5.0
DrissionPage>=4.0.0.0,<5.0.0.0
