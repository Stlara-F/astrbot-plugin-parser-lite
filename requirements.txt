# === AstrBot thin bridge: parser-lite ===
# Install these if missing from your AstrBot environment.

# Card rendering (must-have)
playwright>=1.40
jinja2>=3.0

# Standalone scheduler (must-have)
apscheduler>=3.10

# Image processing
pillow>=10.0

# --- Runtime deps (from upstream) ---
# Most of these are already in AstrBot's venv.
aiofiles>=23.2.1
msgspec>=0.20.0,<1.0.0
beautifulsoup4>=4.12.0,<5.0.0
curl_cffi>=0.15.0,<1.0.0
httpx[socks]>=0.28.0,<1.0.0
DrissionPage>=4.0.0.0,<5.0.0.0
json_repair>=0.58.0,<1.0.0
anyio>=4.0.0,<5.0.0
ujson>=5.0.0,<6.0.0
rich>=15.0.0,<16.0.0
protobuf>=7.0.0,<8.0.0
cryptography>=49.0.0,<50.0.0
